# v2026.8.25 到集成 HEAD：真实搭建 E2E Campaign

## 任务锚点

- **必须结果**：把版本变化拆为 `前一正式版本..v2026.8.25` 与 `v2026.8.25..集成 HEAD` 两段；所有实质改动均有 `change_id`、源码/提交/最终 tree 证据和真实搭建 E2E 映射；由本机 Qoder 执行自然语言搭建；以平台 exact readback 为主证据、已登录浏览器截图为 UI 辅证；产出本周优化报告、矩阵、截图索引、Qoder run 索引、SHA-256 与清理状态。
- **已确认约束**：只使用 `feat/six-capability-frontend-gap-e2e` 对应隔离 worktree；不执行 `npm link`，OpenYida 命令显式使用本 worktree 的 `node bin/yida.js`；不 push、不改 `.gitignore`、不操作 `main/master`；平台资源以本 campaign 的 `runId/marker` 独立 owned；不得泄露认证值或把本地 JSON/截图冒充平台 readback。
- **明确非目标**：不新造通用 Agent 平台；不改写全局 `openyida`；不使用公共回显站点充当连接器 fixture；不模拟第二身份；不通过回退当前集成代码制造旧态截图；不删除 ownership 不明的资源。

## 运行状态

- 深度：`deep`（跨版本、跨能力、真实平台副作用、Qoder 与浏览器双执行面）。
- 运行模式：用户已要求持续执行完整链路；平台副作用只限本 campaign 明确登记的新建/更新资源。组织上下文不能唯一确定、需要第二身份、缺团队受控 HTTPS fixture、删除语义不安全或外部结果 unknown 时，必须转为精确 blocker。
- 集成分支：`feat/six-capability-frontend-gap-e2e`
- 集成基线：`27616a563ef5311e44c71df8653611d4eacc3661`
- campaign runId：`wk-20260828-sixcap-27616a5`
- 当前状态：本地 tag `v2026.8.25` 存在；Qoder CLI 已检测到且处于登录状态；默认任务目录是同提交的 detached worktree，实际操作目录已固定为本分支 worktree。

## 事实分级

- `confirmed`：分支与 HEAD、tag 存在、Node 版本满足 engines、Qoder capability detection 成功、仓库现有真实 E2E/eval harness 与忽略缓存目录可用。
- `inferred`：`v2026.8.25` 的前一正式发布预计为 `v2026.8.20`，必须由 tag 时间、release 元数据和 commit ancestry 共同确认后才能进入报告。
- `confirmed`：用户已在 Chrome 登录“钉钉”组织；owned 应用、3 个表单、12 节点流程、15 节点集成候选、聚合表、报表、发布页和权限只读数据均已由平台 readback/UI 复核。
- `blocked`：连接器缺团队受控 HTTPS fixture；流程 runtime 缺授权第二身份；集成节点未完成配置；聚合表为空配置；报表运行时查询异常；工作台只有 marker；权限未执行 mutation/restore。

## 证据与产物

- 可提交报告：`docs/reports/openyida-weekly-optimization-2026-08-28.md`
- 可提交矩阵：`docs/reports/openyida-v2026.8.25-integration-e2e-matrix.json`
- 可提交索引：`docs/reports/openyida-v2026.8.25-integration-artifact-index.json`
- 原始运行产物：`project/.cache/openyida/wk-20260828-sixcap-27616a5/`（忽略目录，只保存脱敏后可展示的摘要/hash；原始认证值不得落盘）

## Done Contract

1. 变更清单由官方 release notes、本地 tag、commit/diff 和最终 tree 交叉证明，两段边界明确且无已知实质遗漏。
2. 每个 `change_id` 都映射到可复核场景和诚实 verdict；真实平台不可达时只能是 `PLATFORM_PROBE_REQUIRED` 或具体 `BLOCKED_*`，不得标 PASS。
3. Qoder runId、prompt、summary/findings、执行命令、平台结果、浏览器截图、SHA-256、provenance、restore/cleanup 状态可以从索引追溯。
4. 普通表单、流程、集成、报表、聚合表、连接器/权限均有真实资源/UI 证据，或有精确能力/权限 blocker；流程与集成节点数均通过平台 readback + UI 双证据确认大于 10，不能以本地配置替代。
5. focused tests、`check:ci`、`check:skills`、`build:skills`、i18n、`git diff --check`、scope/sensitive scan 和最终 diff review 均有结论；仅在本 feature 分支本地提交，commit message 以 `to #83584385` 结尾，worktree clean，不 push。

## Recap Checkpoint

- 当前状态：`complete_with_blockers`。两段 delta、25 个 change_id、矩阵、Qoder 双 run、真实平台 readback、10 张安全截图、验证与清理账本均已形成。
- 平台成功证据：普通表单 7/7/6 字段与 2/1/1 seed rows；流程 v3、绑定一致、12 节点 designer view；集成控制面 exact readback 与 15 节点数量；权限 2 个包只读回读。
- 精确 blocker：`BLOCKED_SECOND_IDENTITY`、`BLOCKED_CONTROLLED_FIXTURE`、`BLOCKED_INTEGRATION_NODES_UNCONFIGURED`、`BLOCKED_AGGREGATE_EMPTY_CONFIG`、`BLOCKED_REPORT_RUNTIME_QUERY_ERROR`、`BLOCKED_WORKBENCH_MARKER_ONLY`；权限 mutation/import 平台链路保持 `PLATFORM_PROBE_REQUIRED`。
- Qoder 复核：审计 run `20260828-093052-f8d6bb64` 与搭建 run `20260828-093448-c4f51c09` 均 complete。Qoder 的资源/命令/数量证据保留，但其对空配置和占位节点的 PASS 自评已被 owner 独立证据纠正。
- 清理：远端 owned 资源为保留截图且缺全类型安全 exact-delete 契约，明确 `cleanup_blocked`；没有删除或修改非本任务资源。
- 交付入口：`docs/reports/openyida-weekly-optimization-2026-08-28.md`、矩阵和 artifact index；真实页面主入口为 `https://ding.aliwork.com/APP_CTPBSLJ5DFONUFM0NFZ6/workbench`。

## PR 集成闭环 — 2026-08-28

- 核心目标：提交 PR 前证明所有已确认优化均位于 `feat/six-capability-frontend-gap-e2e`，包括六域实现、专项周报会话和登录回调完整界面优化。
- 已发现偏差：`feat/oauth-callback-experience` 的 3 个提交尚未进入集成分支；专项周报会话已直接提交在集成分支。
- 最小修复：只把登录回调体验的 3 个已审查提交串行集成，保留 12 locale、HTML 转义、自动关闭失败回退及跨端浏览器安全语义，不改动 `main/master`。
- Done Contract：源提交均可由 integration ancestry/patch 或最终 tree 证明；集成 worktree clean；OAuth 窄测、六域窄测、release-risk、skills、i18n、`check:ci`、diff/sensitive scan 全部有结论；随后仅推送该 feature 分支并创建目标为 `main` 的 PR。
- 风险与边界：不执行真实登录写入，不改 auth/token 数据，不改 `.gitignore`；Windows/Linux 浏览器真实拉起保留为发布前人工验证项，不以 macOS 单机结果代替。
- 集成结果：OAuth 源提交 `669fae45`、`e0e97f1f`、`425bdd22` 已以 patch-equivalent 提交进入集成分支；六域 final tree 与各源分支一致或仅包含后续已验证的收紧修复；专项周报提交 `aa804966` 为集成分支祖先。
- 验证结果：OAuth 29/29 通过；release-risk 0 error / 2 个跨端人工提醒；skills 与 12 locale 对齐通过；完整 `npm run check:ci` 通过（lint 0 error / 28 warning，全部 Jest、package 与 size gates 通过）；依赖审计保留既有 1 low / 2 high residual。
- PR 状态：已按用户明确授权推送集成分支并创建 PR #523；首轮远端矩阵中 Ubuntu Node 18/20/22/24、Windows Node 20 与 Validation 均通过，macOS Node 20 仅暴露一处测试断言依赖宿主语言的问题。
- macOS 收敛：`integration-spec-builder` 的中文描述断言现在显式切换到 `zh`，并在 `finally` 中恢复原语言；默认环境与 `LANG=en_US.UTF-8` 环境下 focused test 均为 22/22 通过，避免测试污染及平台默认语言差异。
